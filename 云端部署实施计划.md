# 环保简报「双线稳定运行」实施计划

> 借鉴《零成本搭建每日五行穿衣微信推送——WorkBuddy 实战教程》架构
> 目标：本地电脑关机后，每日 08:30 环保简报仍自动抓取到、推送到微信；并与本地 WorkBuddy 线**双线并行、互为备份**

---

## 一、双线架构（核心）

```
┌──────────────────────────────────────────────────────────────┐
│ 线 A · 云线（GitHub Actions + PushPlus）                       │
│   brief_crawl.py（纯Python自抓）→ brief_data_cloud.json        │
│   → render_brief.py → PushPlus → 你微信服务通知                │
│   特点：关机无影响、零积分、纯脚本可复现                        │
└──────────────────────────────────────────────────────────────┘
                              ▲ 互为备份
                              │ （云线抓不到的，本地线用 AI 补；反之亦然）
┌──────────────────────────────────────────────────────────────┐
│ 线 B · 本地线（WorkBuddy 自动化 + WxPusher / AI增强）          │
│   WebSearch（AI检索，比纯脚本更全）→ brief_data.json           │
│   → render_brief.py → WxPusher(App/Topic) + PushPlus兜底       │
│   特点：AI 智能补抓、关机时不跑（故需云线保底）                 │
└──────────────────────────────────────────────────────────────┘

            ★ 两线共用同一套引擎文件（render_brief.py / effective_calendar.json
              / quotes.json / brief_crawl.py），任一处修正自动同步到另一线 ★
```

## 二、两线对比

| 项 | 云线（GitHub+PushPlus） | 本地线（WorkBuddy+WxPusher） |
|---|---|---|
| 运行位置 | GitHub Actions 云端 | WorkBuddy 自动化（依赖本机在线） |
| 关机影响 | 完全无影响 | 不跑（需云线保底） |
| 数据生成 | brief_crawl.py 纯脚本自抓 | WebSearch（AI，覆盖更全） |
| 微信推送 | PushPlus 服务通知 | WxPusher App/Topic（微信通道已失效，仅 App 可达）+ PushPlus 兜底 |
| 数据文件 | brief_data_cloud.json（LINE=cloud） | brief_data.json（默认） |
| 费用/积分 | 免费仓库 + PushPlus 200条/天 | 免费；本地线消耗本机模型积分 |

> 关于 WxPusher 微信通道：WxPusher 官方已宣布微信推送暂时失效，只能推到自有 App。
> 因此**真正到达微信的服务通知由 PushPlus（两线都接）承担**；WxPusher 作为本地线冗余/App 可达通道保留。
> 若未来要微信也双保险，备选：**企业微信 webhook**（不依赖公众号，最稳，需注册企业微信）。

## 三、关键设计决策

### 1. 单一引擎、两线共用
- `render_brief.py` 是渲染+推送**唯一引擎**，含：今日实施模块、双通道推送、旧归档URL归一与守卫。
- 两线只差「数据从哪来」与「PushPlus/WxPusher token 是否就绪」，修正一处两边都生效 → 天然"同步修改"。
- `effective_calendar.json` 是「今日正式实施」的**权威单一来源**，两线同读一份。

### 2. 数据文件分离防冲突
- 云线：`DATA_PATH=brief_data_cloud.json`，输出目录 `日期_cloud/`
- 本地线：默认 `brief_data.json`，输出目录 `日期/`
- 两线不写同一文件、不覆盖同一目录，可同日并行运行。
- `汇总.csv` / `history/items_history.json` 两线共享：history 按 URL 去重，已推过的条目不会被另一线重复推送（反而避免重复）。

### 3. 今日正式实施模块
- `effective_calendar.json` 列出"此前发布、今日才实施"的法规标准（如生态法典 2026-08-15 实施）。
- `render_brief.py` 每日读取，把 `effective_date == 当日` 的条目注入简报**顶部「📌 今日正式实施」**模块。
- 维护：`refresh_calendar.py`（本地按需跑）从法规详情页抽取"自YYYY年MM月DD日起施行"自动补 auto 条目；人工确知的用 curated 条目（不被覆盖）。

### 4. 旧归档栏目（xxgk03）遗留问题处理
- 生态环境部旧框架 `xxgk2018/xxgk/xxgk0X/`（部文件/办公厅文件/规章）已迁至新栏目：
  `/zcwj/bwj/wj/`（部文件）、`/zcwj/bgtwj/wj/`（办公厅文件）、`/ywgz/fgbz/`（法规标准）。
- `brief_crawl.py` 抓取源**只用新栏目**；并加 `LEGACY_ARCHIVE_RE` 守卫——若不慎抓到旧归档链接会高亮告警，防止回退。
- 历史数据（brief_data.json / 汇总.csv）里残留的旧归档**详情页 URL 仍有效，予以保留**（不强行改写，避免改坏链接）；新抓取一律走新栏目。
- 本地线 WebSearch 提示词也要求"优先新栏目链接，旧归档仅作兜底"。

### 5. 时区
GitHub Actions 默认 UTC。`cron: '30 0 * * *'` = 北京时间 08:30。本地自动化同样 08:30（北京时间）。

## 四、实施步骤

**阶段 0｜前置准备（泡沫来做）**
- [ ] 注册 GitHub 账号（免费）
- [ ] 注册 PushPlus（pushplus.plus），微信扫码，完成实名认证，复制 Token
- [ ] 用 PushPlus 先发一条测试，确认微信能收到「服务通知」

**阶段 1｜云线部署（小p 指导）**
- [ ] 建公开仓库（如 `env-brief-daily`），把本目录（含 .github/）上传
- [ ] Settings → Secrets 添加 `PUSHPLUS_TOKEN`（可选 `PUSHPLUS_TOPIC`）
- [ ] 手动 `Run workflow` 测试一次（已配置 playwright + `DATA_PATH=brief_data_cloud.json LINE=cloud`）
- [ ] 连续观察 3-5 天，确认云线稳定

**阶段 2｜双线并行 + 校验**
- [ ] 本地线自动化 `automation-1786766136163`（已更名为"本地线"）保持 ACTIVE
- [ ] 两线同日各跑，核对：今日实施模块一致、PushPlus 微信均可达、WxPusher App 收到本地线
- [ ] 比对两线 汇总.csv，确认互补覆盖（云线抓全自动化源，本地线补 AI 发现的遗漏）

## 五、暂停 / 恢复（重要：暂停≠删除）

| 操作 | 做法 | 代码资产 |
|---|---|---|
| 暂停云线 | GitHub 仓库 → Actions → 该 workflow → Disable workflow（**禁用，不删**） | 全部保留，随时 Enable 恢复 |
| 暂停本地线 | WorkBuddy → 自动化 → 置「已暂停」 | 全部保留，随时恢复 |
| 长期不用 | 两条都暂停即可 | **任何文件都不要删**，日历/引擎/脚本照常维护 |

> 即使两条都暂停，以下仍须按实际修正（万一再次启用）：
> - 新法规上线 → 在 `effective_calendar.json` 增补今日实施条目
> - 官网改版导致抓取失效 → 修 `brief_crawl.py` 栏目/解析
> - WxPusher / PushPlus 通道变化 → 改 `render_brief.py` 推送段
> - 推送通道切换（如改企业微信）→ 在双线同步改

## 六、风险与备案

| 风险 | 说明 | 备案 |
|---|---|---|
| PushPlus 监管风险 | 同走公众号通道，微信收紧时可能受影响 | 备选企业微信 webhook；本地线 WxPusher App 仍可达 |
| 本地线关机不跑 | 本机离线则本地线缺位 | 云线兜底，故必须双线都开着才稳 |
| 抓取质量 | 纯脚本不如 AI 智能，个别源可能漏抓 | 本地线 AI 补抓；关键源可手动补 |
| 官网改版 | 列表页结构变化导致解析失效 | `brief_crawl.py` 加异常告警，失效时微信收到错误提示 |
| 双线重复推送 | 同条目两线各推一次 | history 去重已避免"同 URL 重复"；不同 URL 的互补条目属正常覆盖 |

## 七、待泡沫确认

1. **GitHub 账号**：有还是没有？（没有我指导 3 分钟注册）
2. **PushPlus Token**：注册后发我，我建仓库 + 配 Secrets + 手动验证。
3. 云线是否也接 WxPusher（双通道更冗余）？默认云线只走 PushPlus，本地线走 WxPusher+PushPlus；如需云线也接 WxPusher，把 wxpusher_config.json 一并上传仓库（含 appToken/uid/topic，涉密注意）即可。
